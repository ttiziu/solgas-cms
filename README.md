# Solgas CMS

Panel para administrar las imágenes del sitio Solgas.

## Desarrollo

1. API en `solgas-cms-api` (`./mvnw spring-boot:run`, puerto 8080).
2. En este proyecto, `API_URL` en `.env`.
3. `npm run dev` y abrir http://localhost:3000/login
